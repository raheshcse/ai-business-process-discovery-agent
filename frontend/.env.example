# Base URL of the FastAPI backend, including the API prefix.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# How often, in milliseconds, to poll a running analysis or an
# indexing document. Lower feels more responsive and costs more requests.
VITE_POLL_INTERVAL_MS=2000
